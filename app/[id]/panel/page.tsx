import UserLayout from "@/components/userLayout";

type Props = {};

const UserPanel = (props: Props) => {
  return (
    <UserLayout>
      <div>UserPanel</div>
    </UserLayout>
  );
};

export default UserPanel;
