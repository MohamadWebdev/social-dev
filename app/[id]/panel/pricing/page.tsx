import UserLayout from "@/components/userLayout";

type Props = {};

const Pricing = (props: Props) => {
  return (
    <UserLayout>
      <div>Pricing</div>
    </UserLayout>
  );
};

export default Pricing;
